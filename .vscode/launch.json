{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/api",
            "args": [
                "-dbHost", 
                "localhost",
                "-redocFolderPath",
                "./docs/swagger.json",
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}